<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/sample_gradient">

    <com.igalata.bubblepicker.rendering.BubblePicker
        android:id="@+id/bubblePicker"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:backgroundColor="@android:color/transparent"
        app:layout_constraintBottom_toTopOf="@id/confirmationButton"
        app:layout_constraintTop_toBottomOf="@+id/questionLabel"
        tools:backgroundColor="@android:color/transparent" />

    <TextView
        android:id="@+id/questionLabel"
        style="@style/AppHeadingTextViewStyle"
        android:text="Question Text"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <Button
        android:id="@+id/confirmationButton"
        style="@style/AppButtonStyle"
        android:text="@string/continue_button_label"
        android:alpha="0.2"
        android:enabled="false"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/progressBar" />

    <ProgressBar
        android:id="@+id/progressBar"
        style="@style/AppHorizontalProgressBarStyle"
        android:progress="10"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</android.support.constraint.ConstraintLayout>